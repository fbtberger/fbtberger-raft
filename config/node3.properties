node.id=node3
node.port=9093
data.dir=./data/node3

peer.node1=localhost:9091
peer.node2=localhost:9092
peer.node3=localhost:9093

# Small on purpose, so log compaction (§7) is easy to trigger and observe
# in a demo session -- a real deployment would use something much larger.
snapshot.threshold=20
